/*
//1.1
function num(numbers) {
    let newArrNum = [];
    for (let i = 0; i < numbers.length; i++) {
        newArrNum[i] = (numbers[i] * numbers[i])
    }
    return newArrNum
}
const pushNewArrNum = num([2, 3, 4])
console.log(pushNewArrNum)

//1.2
const num = [2, 3, 4]
const newNumbers = num.map(function (num) {
    return num ** 2;
})
console.log(newNumbers)
*/


